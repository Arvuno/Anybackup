---
type: object_type
id: app_config_rule
name: 应用配置
tags: [备份方案知识网络V2]
---

## ObjectType: 应用配置

用于表达应用配置规则项。每条记录只表达一个应用配置类别，例如 MySQL 的备份方式、即时合成备份、实时日志备份、归档日志删除策略、通道数、自动转完备、页面追踪、自动清理追踪数据、自定义参数等。推荐阶段需按 `config_category` 汇总，并保证同一候选方案的最终应用配置结果中，同一配置类别只出现一次。

### Data Source

| Type | ID | Name |
|------|-----|------|
| data_view | 76367eef-7b72-4b56-b557-4c66cac08a6c | app_config_rule |

### Data Properties

| Name | Display Name | Type | Description | Mapped Field |
|------|--------------|------|-------------|--------------|
| id | 配置规则ID | string | 配置规则唯一标识。 | id |
| app_object_type | 应用类型 | string | 规则适用的应用类型，可为单个类型或多个类型集合。 | app_object_type |
| condition | 附加匹配条件 | string | 除 `app_object_type` 外的附加匹配条件表达式，例如 `target_rpo_minutes <= 60`。 | condition |
| condition_description | 条件描述 | string | 对条件的中文解释。 | condition_description |
| config_category | 配置类别 | string | 应用配置类别，例如 `app.mysql.backupmode`、`app.mysql.realtimelog`、`app.mysql.channel`、`app.mysql.mergebackup`。 | config_category |
| config | 配置内容 | text | 命中规则后应采用的应用配置内容摘要。 | config |
| config_param | 配置参数 | string | 同一配置类别下的参数列表，使用分号分隔，每项采用 `param=value` 形式。 | config_param |

### Logic Properties


### Keys

Primary Key: id
Display Key: id
Incremental Key:
