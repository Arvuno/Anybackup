1. This chart deploys core_agent_service as an MQ worker and does not need external Service or Ingress by default.
2. Enable Service or Ingress only if your environment has an explicit operational need for them.
{{- if .Values.kweaverHostMount.enabled }}
3. The workload mounts the node local KWeaver directory from {{ .Values.kweaverHostMount.hostPath }} to {{ .Values.kweaverHostMount.mountPath }}.
{{- else }}
3. The node local ~/.kweaver mount is disabled in values.
{{- end }}
{{- if .Values.envFile.enabled }}
4. An additional .env fallback file is mounted at {{ .Values.envFile.mountPath }}.
{{- end }}
