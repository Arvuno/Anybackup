1. Get the application access URL:
{{- if .Values.ingressRoute.enabled }}
  The frontend is exposed through the API Gateway Traefik IngressRoute:
  {{ .Values.ingressRoute.pathPrefix }}
{{- else }}
  kubectl port-forward svc/{{ include "web-service.fullname" . }} 8080:{{ .Values.service.port }}
  Then open http://127.0.0.1:8080
{{- end }}

2. Business API routes are not configured by this chart.
  They are exposed by their own service charts through API Gateway.

3. The container health endpoint is:
  http://127.0.0.1/healthz
