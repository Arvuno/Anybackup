Auth service chart installed as a local Keycloak deployment.

Internal service:
  {{ include "auth-service.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local

Keycloak uses external PostgreSQL storage. This chart does not deploy PostgreSQL.

If keycloak.secrets.create=false, create the referenced secret before installing:
  {{ include "auth-service.secretName" . }}

External access:
  This chart owns the Auth Service route rules and forwards to the internal ClusterIP
  service. Keycloak validates Authorization headers directly on protected routes.
