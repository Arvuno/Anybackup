The Traefik API gateway is installed with the ingress class "traefik".

Auth middleware:
  name: {{ include "api-gateway-service.authName" . }}
  namespace: {{ .Release.Namespace }}
  type: tokenToXUser local plugin
  userinfo: {{ .Values.auth.tokenToXUser.userinfoUrl }}

Service routes:
  Defined by each service chart and discovered by Traefik through the CRD provider.
